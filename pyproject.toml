[project]
name = "llm"
version = "0.1.0"
description = ""
readme = "README.md"
authors = [
    { name = "Jon Goikoetxea", email = "jon.goikoetxea.m@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "einops>=0.8.1",
    "equinox>=0.12.2",
    "hydra-core>=1.3.2",
    "jax[cuda12]>=0.6.2",
    "jaxtyping>=0.3.2",
    "optax>=0.2.5",
    "orbax-checkpoint>=0.11.18",
    "regex>=2024.11.6",
    "tensorboardx>=2.6.4",
    "tensorflow>=2.19.0",
    "tqdm>=4.67.1",
]

[project.scripts]
llm = "llm:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
    "tensorboard>=2.19.0",
]

[tool.ruff]
lint.ignore = ["F722"]

[tool.uv.workspace]
members = ["test/potato", "test"]
